apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "equest-server.fullname" . }}-test-connection"
  labels:
{{ include "equest-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "equest-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
