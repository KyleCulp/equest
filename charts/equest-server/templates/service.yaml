apiVersion: v1
kind: Service
metadata:
  name: {{ include "equest-server.fullname" . }}
  labels:
    {{- include "equest-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "equest-server.selectorLabels" . | nindent 4 }}
